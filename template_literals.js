

const aString = 'this is';
console.log(`${aString} pretty much the most difficult thing to do` + ` in ` + `template literals`);
console.log('actually, i \'lied\', this is also a tricky one to note');
console.log("and perhaps \"this one too\"");
console.log("but once you get that \\ is an escape character, then you golden, son");
console.log(`so i learned that template literals are more concerned with \` and not \" or even \', but check 

this out, new line without inserting \\n!!`);